div style="padding: 10px; background: url(#{block.background.thumb}); background-size: cover; color: #{block.inverse_font_color ? '#FFF' : '#000'}"
  h1=sanitize_block block.title
  = sanitize_and_paragraph_block block.text
<section class="case-cart">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="case-row case-cart-address">
      <div class="container globalwidth">
        <div class="wrapper">
          <h2 class="center">Вход для оптовых покупателей</h2>
          <div class="forms">
            <div class="field<%= ' error' if f.object.errors['email'].any? %>"><%= f.text_field :email, placeholder: 'Эл. почта' %></div>
            <div class="field<%= ' error' if f.object.errors['password'].any? %>"><%= f.password_field :password, placeholder: 'Пароль' %></div>
          </div>
        </div>
      </div>
    </div>
    <div class="case-cart-payment">
      <div class="container globalwidth">
        <div class="btn-wrap" style="padding-top: 0">
          <button class="btn btn-orn" type="submit">Войти</button>
        </div>
      </div>
    </div>
  <% end %>
</section>
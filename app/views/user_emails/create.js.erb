
<% if @email_subscription.persisted? %>
  $('form.subscribe-form').hide();
  $('#mailer-thanks').show();
  subscriber.success();
<% else %>
  $('.subscribe-form .msg').text('Произошла ошибка, проверьте email');
  $('#subscriber p.error').show().text('Произошла ошибка, проверьте email');
<% end %>

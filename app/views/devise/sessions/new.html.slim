.row
  .col-lg-4.col-lg-offset-4.m-t-large
    section.panel
      header.panel-heading.text-center
        | Вход
      = form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'panel-body' }) do |f|
        .block
          label.control-label E-mail
          = f.email_field :email, required: false, autofocus: true, class: 'form-control'
        .block
          label.control-label Пароль
          = f.password_field :password, required: false, class: 'form-control'
        - if devise_mapping.rememberable? 
          .checkbox
            label
              = f.check_box :remember_me
              | Запомнить меня
        = f.submit 'Войти', class: 'btn btn-info'
        .line.line-dashed
        a.btn.btn-facebook.btn-block.m-b-small href=user_omniauth_authorize_path('facebook')
          i.icon-facebook.pull-left
          | Войти через Facebook
        a.btn.btn-twitter.btn-block.m-b-small href=user_omniauth_authorize_path('vkontakte')
          i.icon-vk.pull-left
          | Войти через Вконтакте
        .line.line-dashed